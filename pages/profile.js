import Layout from "../components/Layout"

const ProfileView = (props) => {
  return (
    <Layout>
      <h1>Profile View</h1>
      {`propsを受け渡し出力する場所だと思われる。`}
    </Layout>
  )
}

export default ProfileView